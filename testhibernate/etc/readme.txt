hibernate-tutorial

命令提示符
（保证什么都没有的文件夹）
> mkdir testhibernate
> mvn archetype:generate -DgroupId=org.hibernate.tutorial -DartifactId=hibernate-tutorial -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false
（显示BUILD SUCCESSFUL，则创建成功）
> cd hibernate-tutorial

创建Event.java


删除App.java

保存utf-8，不带BOM，用SciTE保存Encoding为UTF-8


